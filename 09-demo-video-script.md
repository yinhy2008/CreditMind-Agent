# CreditMind · Demo 视频录制脚本（2 分钟版）

> 用途：路演中插播的 **2 分钟** 产品演示视频（老师要求 "Demo 视频"）
> 录制工具：macOS 自带 **QuickTime Player**（最稳）；或装好 ffmpeg 后自动录
> 演示前提：Streamlit Demo 已运行（`streamlit run app.py` → http://localhost:8501）
> 录制前请先读 `07-pitch-deck-v0.1.md` 的 S11 Demo 页说明，视频内容需与之呼应

---

## 一、录制准备（5 分钟）

1. 启动 Demo：
   ```bash
   cd 模块四/CreditMind
   streamlit run app.py
   ```
2. 浏览器打开 http://localhost:8501，确认 3 个 Tab 可见（预设 Case / 手动输入 / Agent 访谈）。
3. 录屏软件设置（QuickTime：文件 → 新建屏幕录制）：
   - 分辨率 1920×1080，帧率 30fps
   - 麦克风音量适中，避免环境杂音
   - 开启"鼠标点击高亮"方便观众跟随
4. 准备数据：内置 3 个 Case（李明 / 张伟 / 王强），无需手输。本视频用 **张伟（中风险）** 走完整流程。

---

## 二、逐段脚本（总时长 **2:00**，严格卡点）

> 建议先空跑两遍练手感，再用提词器或打印稿对着讲。

### 0:00–0:20 开场 + 运行推理（20 秒）

> "这是 CreditMind——消费贷 AI 风控 Agent。现在演示一个典型中风险借款人张伟。
> 左侧选'预设 Case 演示'，点张伟，运行推理。"

**操作**：切「预设 Case 演示」Tab → 下拉选「张伟（中风险）」→ 点「运行推理」。
**画面**：鼠标点击动作清晰可见。

### 0:20–1:00 SHAP 解释（40 秒）

> "模型立刻给出违约概率 36.8%，风险等级'中风险'。
> 下面是 SHAP 解释——张伟风险最高的三个因子：近期查询次数偏多、利率偏高、负债收入比偏高。
> 注意，这不是黑盒，每个判断都能说清原因。"

**画面**：定格「违约概率 + 风险等级 + SHAP Top3」，鼠标划过 SHAP 条。

### 1:00–1:40 可解释报告（40 秒）

> "再点'生成尽调报告'，Agent 自动产出 Markdown 报告：
> 客户画像、风险等级、Top3 风险因子、给客户经理的建议话术。
> 原来手写 30 分钟，现在 3 秒。"

**操作**：点「生成尽调报告」→ 滚动展示报告全文。

### 1:40–2:00 收尾（20 秒）

> "CreditMind 不直接放款，只做建议加人审，合规边界清晰。
> 这就是 CreditMind，谢谢观看。"

**画面**：回到 Demo 首页或展示 PPT 致谢页，定格 2 秒。

---

## 三、加分项（如时间允许可替换 1:40–2:00 段）

> 在「Agent 访谈模拟」Tab 输入"我是个体户，想借 15 万装修"，
> 看 Agent 多轮追问年收入、负债、工作年限并实时抽取特征。
> 若未配 `DEEPSEEK_API_KEY`，该模式降级为表单，可口头说明"LLM 模式需配 API Key"。

---

## 四、录制后处理

1. 导出 mp4（H.264，1080p），文件名 `CreditMind-Demo.mp4`。
2. 剪辑：剪掉加载空镜，保留解说连贯；总时长控制在 **1:55–2:05**。
3. 放到仓库 `demo/` 目录（或 GitHub Release）。
4. 路演现场用本地视频兜底，不依赖实时 Demo（防网络不稳）。

---

## 五、常见问题（避免踩坑）

- **Demo 卡顿**：录制前先 `python model_server.py test` 确认模型加载正常。
- **SHAP 图不显示**：确认 `artifacts/` 下模型文件存在，否则先 `python model_server.py train`。
- **解说忘词**：按本脚本打印一份贴屏幕旁，或开提词器。
- **超时**：优先保 0:00–1:40 三段，收尾段可口头带过、画面定格即可。

---

*文档更新：2026-07-25 压缩为 2 分钟版（原 3–4 分钟版废弃）· 作者：尹红艳 · 项目：CreditMind 模块四路演准备*
