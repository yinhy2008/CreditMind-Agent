# CreditMind · Demo 视频录制脚本

> 用途：路演前录一段 3–4 分钟的产品演示视频（老师要求"Demo 视频"）
> 录制工具：macOS 自带 QuickTime Player（或 OBS），录屏 + 麦克风解说
> 演示前提：Streamlit Demo 已运行（`streamlit run app.py` → http://localhost:8501）

---

## 一、录制准备（5 分钟）

1. 启动 Demo：
   ```bash
   cd 模块四/CreditMind
   streamlit run app.py
   ```
2. 浏览器打开 http://localhost:8501，确认 3 个 Tab 可见（预设 Case / 手动输入 / Agent 访谈）。
3. 录屏软件设置：
   - 分辨率 1920×1080，帧率 30fps
   - 麦克风音量调到适中，避免环境杂音
   - 可选：开启"鼠标点击高亮"方便观众跟随
4. 准备 3 个 Case 数据（已内置，无需手输）：李明 / 张伟 / 王强

---

## 二、逐段脚本（总时长 ~3.5 分钟）

### 0:00–0:20 开场（20 秒）

> "大家好，我是尹红艳。这是 **CreditMind**——一个面向消费贷场景的 AI 信贷风控 Agent。
> 今天我用 3 分钟演示：它怎么把一笔 1–2 小时的尽调压缩到 15 分钟。"

**画面**：浏览器最大化，展示 Demo 首页标题 "CreditMind · AI 信贷风控大脑"。

### 0:20–0:50 选择中风险 Case（30 秒）

> "先看一个典型的中风险借款人——张伟。在左侧选 '预设 Case 演示'，点张伟。"

**操作**：切到「预设 Case 演示」Tab → 下拉选「张伟（中风险）」→ 点「运行推理」。

### 0:50–1:40 展示推理结果（50 秒）

> "模型立刻给出违约概率 36.8%，风险等级'中风险'。
> 下面是 SHAP 解释——这是张伟风险最高的三个因子：查询次数偏多、利率偏高、负债比偏高。
> 注意，这不是黑盒，每个判断都能说清楚原因。"

**画面**：定格在「违约概率 + 风险等级 + SHAP Top3」区域，鼠标划过 SHAP 条。

### 1:40–2:20 可解释报告（40 秒）

> "再点'生成尽调报告'，Agent 自动产出一份 Markdown 报告：
> 含客户画像、风险等级、Top3 风险因子、以及给客户经理的建议话术。
> 原来要手写的 30 分钟，现在 3 秒。"

**操作**：点「生成尽调报告」→ 滚动展示报告全文。

### 2:20–3:00 对比实验亮点（40 秒）

> "为什么可信？我们做了竞品对比实验：在 15 万条 LendingClub 数据上，
> 别人用 81 个特征做到 AUC 0.7167，我们用 19 个精选特征做到 0.7093——
> 只低 0.007，但我们排除了数据泄露列、验证了 PSI 稳定性。少即是多。"

**画面**：切到 `experiments/results/comparison_overview.png`（或 PPT Slide 6）展示对比图。

### 3:00–3:30 收尾（30 秒）

> "CreditMind 不直接放款，只做建议 + 人审，合规边界清晰。
> 这就是我的模块四项目，谢谢大家。"

**画面**：回到 Demo 首页，或展示 PPT 致谢页。

---

## 三、加分项：Agent 访谈模式演示（如已配 DEEPSEEK_API_KEY）

> 在「Agent 访谈模拟」Tab，输入"我是个体户，想借 15 万装修"，
> 看 Agent 如何多轮追问年收入、负债、工作年限，并实时抽取特征。
> 若未配 Key，该模式降级为表单输入，可在视频中说明"LLM 模式需配置 API Key"。

---

## 四、录制后处理

1. 导出 mp4（H.264，1080p）。
2. 剪辑：去掉等待加载的空镜，保留解说连贯。
3. 文件名：`CreditMind-Demo.mp4`，放到仓库 `demo/` 目录（或上传 GitHub Release）。
4. 路演时若现场网络不稳，用本地视频兜底，不依赖实时 Demo。

---

## 五、常见问题（录制时避免踩坑）

- **Demo 卡顿**：提前 `python model_server.py test` 确认模型加载正常。
- **SHAP 图不显示**：确认 `artifacts/` 下模型文件存在，或先 `python model_server.py train`。
- **解说忘词**：按本脚本打印一份贴在屏幕旁，或提词器。
- **时长超 4 分钟**：优先保留 0:20–2:20 的核心三段，对比实验可口头带过。

---

*文档生成时间：2026-07-25 · 作者：尹红艳 · 项目：CreditMind 模块四路演准备*
